BINANCE_API_KEY=
BINANCE_SECRET=
# Use host.docker.internal when running in Docker, or localhost for bare-metal
LLAMA_BASE_URL=http://host.docker.internal:11434/v1
LLAMA_MODEL=llama3

# Per-module LLM overrides (optional — falls back to LLAMA_BASE_URL / LLAMA_MODEL)
EXTRACTOR_BASE_URL=
EXTRACTOR_MODEL=
EXTRACTOR_MAX_TOKENS=8192
ANALYST_BASE_URL=
ANALYST_MODEL=
ANALYST_MAX_TOKENS=28000

TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
APPROVAL_TIMEOUT_MINUTES=5
PORT=3000